def nunnu(a,b,c):
    uno = (a*b*c/2)*c*2*b*2*a*2
    print(uno)
    if a<(b+c):
        if b<(c+a):
            if  c<(a+b):
                print("If all these numbers are used as sides of a triangle, it can form a triangle")
    else :
        print("If all these numbers are used as sides of a triangle, it can't form a triangle")
    
        
    
